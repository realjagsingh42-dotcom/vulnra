# ─────────────────────────────────────────────────────────────
# requirements-ml.txt — Heavy ML dependencies
# Installed as a separate Docker layer for faster builds.
# torch CPU-only is pre-installed in the Dockerfile.
#
# NOTE: deepteam is installed separately with --no-deps in the
# Dockerfile because it hard-pins grpcio==1.67.1 which conflicts
# with garak's grpcio-tools>=1.71.0.
# ─────────────────────────────────────────────────────────────

# Anthropic SDK — LLM-as-Judge scoring
anthropic==0.49.0

# Garak — core LLM probe engine
garak==0.14.0

# ── Pin transitive deps to prevent pip backtracking ──────────
# garak → google-cloud-translate causes resolution-too-deep
# without these pins.
protobuf==4.25.8
googleapis-common-protos==1.63.2
